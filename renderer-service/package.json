{
  "name": "signage-renderer",
  "version": "0.1.0",
  "private": true,
  "description": "Headless Puppeteer renderer that turns Lovable template payloads into PNGs and pushes them to Firebase Storage + Firestore.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2",
    "puppeteer": "^23.11.1"
  }
}