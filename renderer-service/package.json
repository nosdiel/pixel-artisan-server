{
  "name": "signage-renderer",
  "version": "0.2.0",
  "private": true,
  "description": "Node-side Fabric renderer that turns Lovable template payloads into PNGs and pushes them to Firebase Storage + Firestore.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2",
    "fabric": "7.3.1",
    "canvas": "^2.11.2"
  }
}
